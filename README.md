# Customer-Accounts Management System (CAMS) CLI Application

## Prerequisites  
- Runtime/SDK: **Java 24**
- Build Tool: **Maven 3.9+**

## How to Build the Executable Artifact
Run the following command in the root directory to compile and package the application:
```bash
mvn clean package
